# 统一换行符，避免「Windows 上开发、Linux CI 上构建」产生整文件级的假 diff。
# 默认 LF：
* text=auto eol=lf

# 但 .bat 必须保持 CRLF —— cmd.exe 对 LF 换行的批处理容错很差，
# 某些结构（如带 label 的 goto）会直接失败。
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.pdf binary
